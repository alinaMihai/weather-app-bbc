import styled from 'styled-components';

export const StyledLocations = styled.ul`
    & {
        list-style: none;
        display: flex;
    }
    button {
        background: none;
        border: none;
        cursor: pointer;
    }
`;
